# Compile
objc2dart(functions)
objc2dart(type-definitions)
objc2dart(comma-separated-expressions)

# Test
add_diff_test(functions)
add_diff_test(type-definitions)
add_diff_test(comma-separated-expressions)

